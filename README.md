# Python-Experiment-03
Five beginner Python scripts covering core control-flow concepts: printing a multiplication table, summing the digits of an integer, checking for a leap year, and generating two numeric patterns using nested loops. All programs run interactively in the terminal and require only the Python 3 standard library.
